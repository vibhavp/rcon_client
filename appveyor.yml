clone_folder: c:\gopath\src\github.com\vibhavp\rcon_client

environment:
  GOPATH: c:\gopath

install:
  - echo %PATH%
  - echo %GOPATH%
  - go version
  - go env
  - go get -v -t ./...

build_script:
  - go build -v -o rcon_client

artifacts:
  - path: rcon_client